{
  "name": "uccc-genomics-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "rm -rf src/.observablehq/cache && observable build",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
